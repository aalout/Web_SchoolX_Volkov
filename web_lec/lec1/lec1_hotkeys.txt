ctrl + ё = Терминал
ctrl + shift + p = Поиск по командам

Функции

function sum(a, b){
    return a + b;
}


СТРЕЛОЧНАЯ ФУНКЦИЯ, В ЧЕМ РАЗНИЦА (ее не будет видно до ее появления)
let sum = (a, b) => {
    return a + b;
}

Обозначения

var, let, const
let, const - блоччные
var - функциональная (можно вызвать в любой точке кода) (не использовать)

#/................ДЗ................\#
Что в nest js в документации читать
Introduction
Overview ----- First steps ------ Controllers ------- Providers ------- Modules

